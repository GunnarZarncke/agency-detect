# agency-detect (core)

Core unsupervised agent discovery: decoupled simulators, lagged-MI clustering, Markov-blanket validation, and CLI entry points.

```bash
pip install -e .
pytest
python -m agency_detect.cli --help
```

See the repository root `README.md` for the full UAD overview.
