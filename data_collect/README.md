# data-collect

Real-machine trace collection for E19 intention-detection experiments (`psutil` workers, burn-in, run packing).

Used by `intention_detect/scripts/run_machine_dataset.py`. Install with `pip install -e .` from this directory.
