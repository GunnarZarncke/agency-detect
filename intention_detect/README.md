# intention-detect

Outcome-influence scoring, segmentation, and defense probes on agent telemetry. Runners live in `scripts/`; real-machine collection uses the sibling `data_collect` package.

```bash
pip install -e ../learn_agents -e .
pytest
```
