#!/usr/bin/env python
"""
Setup script for backward compatibility.

Install the core package with:

    pip install -e agency_detect

For the full monorepo, install each top-level package editable (see root README).
"""

from setuptools import setup

setup()
